node_modules
dist
dist-ssr
*.local

# .env коммитится: там только публичные значения (см. комментарий внутри).
.env.local
.env.*.local

.vercel
supabase/.temp
supabase/.branches

*.log
npm-debug.log*
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
.playwright-mcp

# Vercel вырезает .env-файлы из исходников при сборке — публичные значения
# лежат в src/config.public.ts, а .env остаётся только для локальных переопределений.
.env
